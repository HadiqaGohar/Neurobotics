{
  "version": 2,
  "builds": [
    {
      "src": "book-frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/book-frontend/build/$1"
    }
  ]
}